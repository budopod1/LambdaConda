for ([-1, 0, 1], (v1: float) {
    for ([-1, 0, 1], (v2: float) {
        print(v1 + ", " + v2)
        print(">" + (v1 > v2))
        print(">=" + (v1 >= v2))
        print("<" + (v1 < v2))
        print("<=" + (v1 <= v2))
    })
})
